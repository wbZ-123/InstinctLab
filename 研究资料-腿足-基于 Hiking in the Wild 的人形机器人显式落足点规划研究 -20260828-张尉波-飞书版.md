# 研究资料-腿足-基于 Hiking in the Wild 的人形机器人显式落足点规划研究 -20260828-张尉波

> 文档日期：2026-08-28
> 作者：张尉波
> 项目仓库：[wbZ-123/InstinctLab](https://github.com/wbZ-123/InstinctLab/tree/feat/foothold-01-flat-tracking)
> 当前分支：`feat/foothold-01-flat-tracking`

## 一、研究背景与目标

### 1.1 研究背景

Hiking in the Wild 原有方案主要通过深度视觉、AMP 和低层动作策略隐式完成落足。该方案可以产生行走动作，但下一步具体落在哪里不容易解释，也难以单独检查落足点是否越过可达范围、是否靠近台阶边缘，以及摆动轨迹是否会碰撞地形。

本研究在原有 G1 parkour 行走任务上增加显式落足点规划模块，将“下一步踩哪里”作为可观测、可约束和可训练的中间变量。规划结果仍由原有电机动作策略跟踪，从而保留原 Hiking 的动作生成能力，同时增加落足点诊断、安全检查和后续实机迁移所需的接口。

### 1.2 研究目标

本研究聚焦以下技术问题：

1. 在平地、台阶和复杂 parkour 地形中生成明确的下一摆动脚落足点；
2. 将落足点的水平位置、世界地形高度、脚掌外围安全和摆动轨迹净空统一起来；
3. 让学习式 planner 根据深度视觉、名义落点和机器人状态，对解析名义点进行有限调整；
4. 将最终落足点和摆动参考轨迹闭环输入低层电机策略；
5. 区分规划失败、提前触地、支撑丢失和 Recovery，形成可复现的诊断流程。

当前目标是完成仿真中的显式规划闭环和可量化验证。实机部署、不同机器人重新标定以及所有楼梯场景的稳定收敛尚未完成，因此本文不将当前版本表述为已经完成实机可靠落足。

### 1.3 量化指标与验收口径

当前版本将“设计约束”“阶段性结果”和“最终性能”分开记录，避免把安全阈值误写成算法性能提升。

| 类别 | 指标 | 数值或统计口径 | 当前状态 |
|---|---|---:|---|
| 设计约束 | 落足点高精度目标 | 水平误差不超过 `0.02 m` | 已写入奖励目标 |
| 设计约束 | 摆动落地验收范围 | 水平误差 `0.08 m`、高度误差 `0.06 m` | 已由状态机使用 |
| 设计约束 | 两脚最大允许高度差 | `0.27 m` | 已由几何检查使用 |
| 设计约束 | 摆动时长 | `0.32 s` | 当前 G1 标定值，仍需跨机器人复核 |
| 设计约束 | 摆动最高点上限 | `0.14 m` | 已由轨迹预检使用 |
| 软件回归 | 落足模块测试 | `440 passed` | 已完成 |
| 阶段性结果 | planner 路由成功率 | 现有记录约 `99.9%` | 仅代表软件链路，不代表楼梯通过率 |
| 阶段性结果 | 不安全名义点修正成功率 | 后期记录约 `67%` | 仍需统一条件复测 |
| 阶段性结果 | 学习落点安全率 | 后期记录约 `97%` | 仍可能存在轨迹中段碰撞 |
| 待测性能 | 平地/上楼/下楼成功率 | 统一 checkpoint、种子和地形后统计 | 尚未完成 |
| 待测性能 | 落足水平误差均值及 95 分位数 | 以实际触地位置相对目标点计算 | 尚未完成 |
| 待测性能 | 提前触地率、Recovery 进入率 | 按摆动事件统计 | 尚未完成 |
| 待测性能 | 相对原版的提升幅度 | 原版与显式 planner 做同条件 A/B | 尚未完成 |

其中前九项是当前可以追溯的数值，后四项是正式验收必须补采的量化结果。在统一实验完成前，不使用“提升了多少百分比”之类的结论性表述。

## 二、研究方法与技术体系

### 2.1 问题定义与数学建模

每次规划在冻结的支撑脚坐标系中进行。设支撑脚世界位置为 o_w，身体水平朝向为 ψ，期望速度为 v^*，学习式 planner 输出水平落足点 p_f = (x_f, y_f)。

本节公式采用纯文本上下标写法，以兼容飞书 Markdown 导入。例如 p_f^nom 表示“名义落足点”。

解析模块首先生成名义点：


p_f^nom = g(v^*, T_lookahead, w_step, swing_side)


其中 T_lookahead 由摆动时长和预瞄相位推导，w_step 是名义步宽，swing_side 表示当前摆动腿。

学习式 planner 输出的是最终水平位置，而不是修正量。输出经过归一化后映射到可达椭圆：


(x_f / r_x)^2 + (y_f / r_y)^2 ≤ 1


局部点变换到世界坐标：


p_xy,w = o_xy,w + R_z(ψ) × p_xy,f


系统只使用世界坐标 x_w、y_w 查询地形网格得到 z_w，最终落足点为：


p_w = (x_w, y_w, z_w)


目标点需满足地形高度有效、两脚高度差不超过当前配置上限、可达椭圆约束，以及脚掌外围安全和摆动轨迹净空要求。

### 2.2 整体技术框架

```text
头部深度图 ─────────────┐
IMU/关节/上一动作 ───────┼→ 学习式落足 Planner → XY 落足点
速度命令/步态状态 ──────┘                         ↓
解析名义点 ─────────────────────────────→ 世界坐标地形查询 → Z
                                                        ↓
                              脚掌外围/危险圆柱/可达性/轨迹预检
                                                        ↓
                                    锁定目标和摆动参考轨迹
                                                        ↓
                         原有低层 29 维 MoE 电机策略跟踪
```

学习训练阶段包括：

1. Isaac Lab 生成多环境平地、台阶及 parkour 地形；
2. 解析 planner 生成名义落足点；
3. 学习式 planner 输出二维水平落足提案；
4. 仿真地形和危险圆柱提供安全评分、轨迹检查及诊断标签；
5. `reward_0` 更新电机执行分支，`reward_1` 在落足事件上更新 planner 分支；
6. 使用事件门控 PPO，避免同一规划动作在多个控制步重复计数。

推理应用阶段不改变原有电机动作输出接口。planner 输出的 XY 经世界坐标地形高度查询后生成摆动轨迹，并作为低层策略的目标观测。

### 2.3 核心算法逻辑

单次落足事务执行步骤如下：

1. 根据真实接触关系确定支撑脚和摆动脚；
2. 在 HOLD 建立事务，冻结支撑脚世界位置、身体朝向和本步速度命令；
3. 解析模块生成名义落足点；
4. planner 读取深度图、机器人状态、名义点和步态状态，输出绝对 XY；
5. 将 XY 转为世界坐标并查询世界地形高度；
6. 检查高度差、可达椭圆和数值有效性；
7. 用脚掌最底层外围采样点检查危险圆柱侵入；
8. 在 HOLD 中生成完整摆动轨迹并进行起点、终点和中段净空预检；
9. 必要时提高摆动最高点，当前上限为 `0.14 m`；
10. 预检通过后锁定轨迹和目标，才允许进入 SWING；
11. 电机策略跟踪轨迹，接触确认后更新支撑脚关系；
12. 若发生物理异常，暂停普通 planner，进入 Recovery，稳定后重新建立 HOLD 事务。

伪代码：

```text
HOLD:
    冻结支撑坐标系和速度命令
    生成名义落点
    planner 输出绝对 XY
    查询世界地形高度得到 Z
    检查几何可达性和脚底安全
    生成并预检摆动轨迹
    若全部通过：锁定事务，进入 SWING
    否则：保留事件奖励，等待下一次合法提案

SWING:
    只使用已锁定的支撑坐标、起点、终点和轨迹
    不用实时支撑脚位置重新解释旧目标
    触地后更新左右脚角色

RECOVERY:
    暂停普通学习式落足规划
    由动作策略恢复身体和接触关系
    恢复稳定后返回全新的 HOLD 事务
```

### 2.4 核心数学公式

安全余量使用脚掌外围点到危险圆柱表面的有符号距离。完全没有侵入时，安全余量随最小净空增加而增大；发生侵入时，使用侵入点比例和总侵入深度形成负分：


S_safe = −(0.5 × r_penetration + 0.5 × d_penetration,norm)


规划奖励在事件发生时计算，并限制到 `[-1, 1]`。典型组合为：


R_plan = w_s × S_safe + w_p × S_progress + w_l × S_step + w_n × S_nominal


其中：

- S_safe：脚掌外围侵入安全分；
- S_progress：沿冻结速度方向的有符号前进分，后退为负；
- S_step：实际步长与命令预测步长的一致性；
- S_nominal：学习点与名义点的接近程度；
- w_s、w_p、w_l、w_n：不同名义安全状态下的分支权重。

名义点接近项使用 2 cm 死区：完全一致为 `+1`，0～2 cm 连续下降到 `0`，超过 2 cm 后继续下降，达到归一化可达尺度时接近 `-1`。

低层摆动位置跟踪采用指数形式：


R_swing = exp(− ||p_foot − p_ref||^2 / σ_pos^2)


当前摆动位置跟踪带宽为 `0.05 m`，摆动速度跟踪带宽为 `0.05 m` 等效尺度，落地跟踪带宽为 `0.10 m`。这些奖励服务于电机策略，不等同于 planner 的 `reward_1`。

### 2.5 仿真、训练与 Sim2Real

仿真环境为 Isaac Sim 5.1 / Isaac Lab，任务为 Unitree G1 parkour AMP。当前典型训练规模为 4096 个环境，训练命令和数据路径由 `scripts/foothold_train.sh` 管理。运动参考数据及筛选 YAML 不在 Git 仓库中。

当前关键配置（以源码为准）：

| 参数 | 当前值/说明 |
|---|---|
| 电机动作 | 29 维 |
| planner 动作 | 2 维绝对水平 XY |
| planner MLP | `[128, 64]` |
| planner 深度特征 | 64 维 |
| 默认摆动时长 | `0.32 s` |
| 训练任务 startup/reset hold | `0.15 s` |
| 最大目标高度差 | `0.27 m` |
| 默认摆动最高点 | `0.08 m` |
| 自动抬高上限 | `0.14 m` |
| 当前 no-fly 权重 | `-3.0` |
| 可达椭圆试验半轴 | 前后 `1.00 m`、左右 `0.25 m`，仍需严格标定 |

主要奖励包括速度和姿态保持、AMP、动作平滑、摆动位置/速度跟踪、落地跟踪、提前触地、未离地、摆动高度不足、轨迹侵入、Recovery、no-fly 和终止惩罚。当前 `reward_0` 是电机执行奖励，`reward_1` 是落足规划事件奖励。

Sim2Real 的主要差异是：仿真可直接访问地形网格、危险圆柱、脚底外围点和精确接触力，而真实机器人只能获得深度图、IMU、编码器和状态估计。实机部署必须用深度图/局部地图替代仿真地形真值，用接触估计替代 PhysX 接触力，并重新标定相机外参、延迟、脚底尺寸、可达范围和触地阈值。

### 2.6 代码资源链接

代码仓库：<https://github.com/wbZ-123/InstinctLab/tree/feat/foothold-01-flat-tracking>

主要入口和文件：

- `source/instinctlab/instinctlab/sensors/foothold_planner/foothold_planner.py`：规划主编排；
- `source/instinctlab/instinctlab_foothold/target_search.py`：脚掌外围和危险圆柱安全计算；
- `source/instinctlab/instinctlab/learning/independent_foothold_actor_critic.py`：29+2 策略结构；
- `source/instinctlab/instinctlab/learning/foothold_depth_encoder.py`：planner 专用深度编码器；
- `source/instinctlab/instinctlab/learning/event_gated_foothold_ppo.py`：事件门控 PPO；
- `source/instinctlab/instinctlab/tasks/parkour/config/parkour_env_cfg.py`：任务、观测、奖励和参数；
- `scripts/foothold_train.sh`：训练入口；
- `scripts/foothold_play_step.sh`：台阶专用 Play；
- `tests/parkour/foothold/`：规划、奖励、状态机、日志和配置测试。

checkpoint、TensorBoard 日志、运动参考数据和 Isaac Sim 不在 Git 仓库中，需要单独归档。

## 三、实验设计与结果对比

### 3.1 对比实验方案

建议至少保留以下对照组：

1. 原版 Hiking/InstinctLab：无显式 planner；
2. 解析名义落点版本：只使用解析 planner，不使用学习式调整；
3. 显式 planner 无专用深度编码器版本；
4. 当前显式 planner + 专用深度编码器版本；
5. 必要时进行 planner 奖励分支平衡、相机输入和可达椭圆半径的消融实验。

每组实验应固定机器人模型、运动参考数据、环境数量、随机种子和评估地形，避免把训练时长或地形课程差异误认为算法收益。

### 3.2 实验基础设置

- 机器人：Unitree G1；
- 任务：平地、正向台阶、反向台阶及 parkour 地形；
- 仿真：Isaac Sim 5.1、Isaac Lab；
- 训练框架：原有 AMP + MoE 电机策略，新增独立 planner PPO 分支；
- 训练规模：常用 `NUM_ENVS=4096`；
- 视觉：躯干深度相机，经过裁剪、归一化和帧延迟处理；
- 数据：AMASS/parkour motion reference 及筛选 YAML；
- 硬件记录：历史训练日志中使用过 RTX 5880 Ada 48 GB、AMD EPYC 平台；具体复现实验应以运行机器记录为准。

### 3.3 实验结果

截至 2026-08-28，仓库交接文档记录的客观结果如下：

| 指标 | 当前证据 | 结论 |
|---|---:|---|
| 落足模块完整测试 | `440 passed` | 软件逻辑回归通过 |
| planner 评估频率 | 约占控制步 `4.7%` | 事件天然稀疏 |
| 名义点不安全事件 | 约占 planner 事件 `8.7%` | 困难样本比例较低 |
| 不安全名义点修正成功率 | 后期约 `67%` | 已具备一定修正能力，仍不完整 |
| 学习点整体安全率 | 后期约 `97%` | 大部分提案通过安全评价 |
| planner 路由成功率 | 约 `99.9%` | 软件路由链路基本可用 |
| `reward_1` | 约从 0 上升至 `0.33` 后平台 | 普通安全点较稳定，困难点仍需训练 |
| 实机验证 | 未完成 | 不得宣称已完成 Sim2Real |

上述数据来自现有训练和 Play 记录，不能替代在统一随机种子、统一地形和统一 checkpoint 下的正式对比实验。楼梯稳定选点、低层摆动跟踪、提前触地和 Recovery 仍是当前重点。

### 3.4 问题诊断与调参经验

#### 3.4.1 常见问题

- 楼梯前停步：先区分 planner 没有提案、提案几何无效、端点不安全、轨迹预检失败和 Recovery；
- 双脚小跳：优先检查提前触地、实际空中时间、支撑脚丢失和低层轨迹误差；
- 目标点看似不更新：区分规划事件频率、HOLD 锁定、Recovery 和 Marker 可视化；
- `reward_1` 下降：同时查看安全/不安全事件比例、修正成功率、平均修正距离、planner KL 和标准差，不能只看单条曲线。

#### 3.4.2 核心监控信号

推荐按以下顺序排查：

```text
是否产生 planner 事件
→ 提案是否有限且几何有效
→ 端点安全是否通过
→ 完整摆动轨迹预检是否通过
→ 最终路由采用哪一个点
→ 实际脚是否跟踪轨迹
→ 是否提前触地/支撑丢失
→ Recovery 是否能够退出
```

#### 3.4.3 调参顺序

先确认坐标系、接触状态和支撑脚快照，再检查电机姿态/轨迹跟踪，最后才调 planner 奖励、可达椭圆和课程参数。不要同时修改状态机、奖励权重、网络结构和地形参数。

#### 3.4.4 安全边界

- 世界高度查询不能改为局部坐标直接查询；
- 进入 SWING 后不得改变支撑脚、轨迹起点、目标和身体朝向快照；
- 几何无效和安全分低必须区分；
- 不得为了提高通过率删除脚底外围或轨迹安全检查；
- `no-fly`、最大步高和接触判断属于安全相关参数，修改必须有独立 A/B 实验和回归测试；
- 实机部署前不得把危险圆柱仿真真值当作可用传感器。

## 四、研究结论

### 4.1 核心优势

当前实现完成了从解析名义落点到学习式绝对 XY、世界高度补齐、脚底安全检查、摆动轨迹生成、低层策略跟踪和事件奖励更新的完整软件闭环。相比完全隐式的落足行为，当前方案具备以下工程优势：

- 落足点成为可记录、可视化和可诊断的中间变量；
- 坐标变换、地形高度、可达性和脚底安全可以独立检查；
- planner 与电机策略参数隔离，避免 planner 更新直接破坏电机 MoE；
- 事件门控 PPO 保持落足奖励和规划动作的一一对应；
- 通过统一日志可以把 planner 选点问题与低层轨迹跟踪问题分开。

以上优势由代码测试和仿真链路验证支撑，不等同于所有复杂地形上的性能已经收敛。

### 4.2 仍存在的不足

#### 4.2.1 典型失败案例

- 台阶边缘附近的名义点不安全时，学习式 planner 可能仍然修正不足或修正距离过大；
- 脚端点安全不代表整条摆动轨迹一定安全，中段 clearance 仍可能失败；
- 低层策略存在提前触地、摆动误差和 Recovery 进入；
- 头部深度相机可能看不到脚边的完整可落区域；
- 仿真危险圆柱和精确地形网格无法直接迁移到实机。

#### 4.2.2 整体短板

- 困难的非安全名义点事件比例较低，planner 学习信号稀疏；
- 标量安全分只提供结果，不直接给出几何方向；
- 当前前后可达半径 `1.00 m` 仍是试验值，可能过于宽松；
- 尚未完成统一条件下的楼梯成功率、落足误差和实机指标统计；
- Recovery 和单支撑事务仍需更长时域验证。

#### 4.2.3 技术债务

- 脚底尺寸、步宽、可达椭圆、摆动时长和触地阈值仍有临时参数；
- 危险圆柱是仿真安全监督，不是实机可直接获得的输入；
- 部分历史文档和实验日志对应旧版本，必须以当前源码和 Git 提交为准；
- checkpoint、运动数据和运行环境没有随 Git 归档；
- 单支撑 Recovery 的地形高度中位数方案尚未完成。

#### 4.2.4 已尝试但效果不稳定的方案

- 直接把名义点随机扰动和 planner PPO 探索同时增大，会使基准不稳定；
- 只提高摆动最高点不能解决终点落足点位于危险边缘的问题；
- 只依赖安全分会诱导 planner 选择远离边缘但不符合前进意图的点；
- 只看 `reward_1` 无法区分安全事件占比变化和真正的困难样本学习效果。

## 五、后续优化计划与展望

1. 在统一地形、随机种子和 checkpoint 下，完成 planner、低层跟踪和 Recovery 的分层指标统计；
2. 重新根据 G1 运动学和实际策略站姿标定可达椭圆、步宽、摆动时长和目标高度差；
3. 提升头部深度图与脚前方落足区域的空间对齐，验证视觉遮挡对楼梯选点的影响；
4. 对非安全名义点进行事件级样本分析，必要时再评估 planner PPO 的类别平衡；
5. 将仿真特权安全监督逐步替换为深度图局部地图、接触估计和可部署的足端安全检查；
6. 完成实机前的延迟、噪声、相机外参和执行器误差随机化；
7. 在实机部署前保持最终安全监督，不以训练通过率为理由删除几何和轨迹检查。

## 六、资料完整性确认清单

- [x] 研究资料主体已整理
- [x] 系统架构和数据流已说明
- [x] 输入输出定义已说明
- [x] 核心算法和公式已说明
- [x] 核心参数和参数来源已记录
- [x] 代码仓库和关键入口已记录
- [x] 当前分支和 Git 基线已记录
- [x] 仿真测试入口已记录
- [ ] 完整统一对比实验结果
- [ ] 全部 checkpoint 和 TensorBoard 数据归档
- [ ] 实机复现
- [ ] 实机安全边界验证
- [x] 已知问题和技术债务已整理
- [x] 后续优化路线已整理

## 附录：常用复现命令

全量测试：

```bash
cd ~/InstinctLab-foothold
PYTHONPATH="$PWD/source/instinctlab:$PYTHONPATH" \
python -m pytest -q tests/parkour/foothold
```

学习式 planner 短训：

```bash
ENABLE_LEARNED_FOOTHOLD_PLANNER=1 \
RUN_NAME=planner_depth_encoder_4096env_100it \
NUM_ENVS=4096 \
MAX_ITERATIONS=100 \
SAVE_INTERVAL=50 \
./scripts/foothold_train.sh 2>&1 | tee logs/planner_depth_encoder_4096env_100it.txt
```

学习式 planner 长训：

```bash
ENABLE_LEARNED_FOOTHOLD_PLANNER=1 \
RUN_NAME=planner_depth_encoder_4096env_30000it \
NUM_ENVS=4096 \
MAX_ITERATIONS=30000 \
SAVE_INTERVAL=2000 \
./scripts/foothold_train.sh 2>&1 | tee logs/planner_depth_encoder_4096env_30000it.txt
```

台阶 Play：

```bash
LOAD_RUN=<运行目录名> \
FOOTHOLD_DEBUG_INTERVAL=20 \
FOOTHOLD_DEBUG_ENV_IDS=all \
FOOTHOLD_CURRICULUM_SCALE_OVERRIDE=1.0 \
./scripts/foothold_play_step.sh \
  --checkpoint model_<轮数>.pt \
  --video_length 3000 \
2>&1 | tee logs/foothold_play_<轮数>.txt
```
